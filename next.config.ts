import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com','ferf1mheo22r9ira.public.blob.vercel-storage.com','images.unsplash.com'],
  },
};

export default nextConfig;
